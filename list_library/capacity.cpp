#include <bits/stdc++.h>
using namespace std;
int main()
{
    list<int> myList = {1, 2, 3};

    // to find how much size it can contain
    // cout<<myList.max_size();


    // to clear the list
    // myList.clear();

    // to resize the list
    // myList.resize(5, 100);

    return 0;
}